import './Header.css';

const Header = () => (
  <nav className="page-navbar navbar navbar-expand-lg bg-body-tertiary">
    <div className="container-fluid">
      <a className="navbar-brand" href="#">AI Task Manager</a>
    </div>
  </nav>
);

export default Header;
